# 通信物理层：单载波 PSS 与 OFDM SSS 信号的解调与同步处理流程 (409.6MHz -> 60MHz)

本文档旨在介绍从原始宽带数字基带信号（IQ 数据）中，同时提取并精细解调 **单载波 PSS（主同步信号）** 与 **多载波 OFDM SSS（辅助同步信号）** 的完整数字信号处理（DSP）接收机链路。全流程涵盖了频移、闭环重定时、高精度多项式降采样、载波恢复、以及信道级相位消除等专业 DSP 技术手段。

---

## Part 1. 单载波 PSS (SDPSK) 盲捕获与自适应长距同步

PSS 作为链路的先锋信号，具有抗干扰强的恒包络 SC（Single Carrier）特性，其承载数据呈现明显的重复块特征（极长段的 M 序列），且调制方式为 $\pi/2$-BPSK (SDPSK)。针对该信号盲检测困难、频偏及多普勒偏大的特点，设计了严格的锁频重定解算流。

### Step 1.0: 基于时域差分相关 (Slide-128 Blind Search) 的位置盲捕
- **需求**：由于 SSS 起始点是由前置的 PSS 探测定位决定的，在无先验信号且混有强烈底噪长达百万级别采样的宽带文件中，第一步需要找出 PSS 藏在哪里。
- **操作 (Slide 128)**：基于 PSS 含有周期为 **128 (重采样对齐后或已知间隔系数)** 个符号复用的结构，设计一种名为 Slide-128（滑窗滑动128周期差分自动相关）的盲搜法：
  1. 通过截取窗内样本数据与延后一个周期的自我样本执行 `Delay-and-Multiply (D&M)` 延迟点乘。
  2. 当滑窗进入 PSS 起始处时，由于内部自编码完全吻合，所有乘积矢量的相位将猛烈聚合并向上抬升出巨大的相关性脉冲。
  3. 因为不需要已知本地 m 序列即可引发共振，该算法可在重度频偏、甚至低 SNR（信噪比）的环境下瞬间捕捉到第一块 `m 序列` 的切确起始跳变峰。通过该粗步物理位置探测，即可锁定 PSS 整个帧头基准并圈定提取块(ROI)位置！

### Step 1.1: 频谱下变频 (DDC) 与 粗降采样
- **操作**：首先将 $409.6\text{ MHz}$ 下的原始 IQ 序列乘上 $e^{-j \cdot 2\pi \cdot 63\text{MHz} \cdot t}$ 平移到真实基带，消除固有的 $63\text{ MHz}$ 物理频距偏移。接着使用多相抗混叠重采样 (`resample`) 函数，将信号压降到 $540\text{ MHz}$ ($9\times$ 过采样)，此时信号进入同步环路预备状态。

### Step 1.2: 自适应幅度归一化防震荡
- **操作**：鉴于突发 (Burst) 信号中大片死区会导致均值功率极小、信号段功率极大，传统的基于 RMS（均方根）的归一化会“引爆”后续闭环误差检测器的增益边界。系统自主评估 99% 峰值势能与 RMS 阈值的比值，执行自适应门限归一化，护航控制闭环（Loop Filter）。

### Step 1.3: Farrow + Gardner 闭环系统重定时 (SRO)
- **操作**：用全软件实现了原本硬件基带才涉及的双环路锁相机制。
  1. 使用 **Gardner TED（定时误差检测器）** 以公式 $\Re\{(y[k] - y[k-1]) \cdot y^*[k-0.5]\}$ 持续捕捉眼图跳变界面的游走。
  2. 把得到的误差通过比例积分 (**PI**) 环路滤波器获得控制量，反向更新 **NCO 星阶步进**。
  3. 控制 **Cubic Farrow 插值器** 输出，吐出完美对应在最优采点瞬间的 SPS=2 符号与中间带点。

### Step 1.4: 混合频偏补偿 (M次幂粗估 + Costas环精跟)
- **操作**：为消除晶振频偏 (CFO)，先通过 M=4 的次方和 FFT 搜索进行**粗步扫描**得到宽段频偏，随后引入 **科斯塔斯环 (Costas Loop)** 进行极精细的相位追踪与多普勒补偿，使原本疯狂旋转的散落圆环迅速凝聚为标准的 4 个极点！

### Step 1.5: 跨界抗倒相差分解调
- **操作**：面对 M 序列中的 Block Phase Inversion 问题，采用前向共轭差分检波 $y[n] \cdot y^*[n-1]$。其巧妙避开了绝对相位极性模糊的问题，天然剔除了模块反相机制。将最终判决直接展开为极其稳定且可直接映射解码出的 M 序列 Hex 十六进制码流。

---

## Part 2. OFDM SSS 信号降频对齐与星座恢复

SSS 采用抗多径信道的 OFDM (带 CP 的多载波技术) 并使用 4QAM 调制传输帧信息，其解调重在基于前一阶段提取出的参数实现微观对齐（Time/Phase Adjustment in Subcarrier Domain）。

### Step 2.1: 复用架构的 DDC 下变频与 CFO 吸收
- **操作**：复刻 PSS 的预推流手段，将 $409.6\text{ MHz}$ 信号再次拨回基带零频 ($63\text{ MHz}$ 右移回)，由于 PSS Gardner 阶段已极为精确地吐出了 **CFO 载波残偏** (例如 `17556 Hz`) 分量，这部分微偏作为已知项乘入基带直接剥除。

### Step 2.2: 零相移抗混叠降频前置
- **优化亮点**：要跨入 OFDM 符号操作所在的 $60\text{ MHz}$ 空间。此时不再使用粗糙的 FIR 或者自带滤波会改变原始起点的 `resample` 工具，我们改用 **`filtfilt` 双向零相移滤波器** 砍掉 $35\text{ MHz}$ 以上的频段，高频干净阵亡且所有的波形鼓包起点的**绝对时间态 0 取移**！

### Step 2.3: Farrow 多项式平滑切降
- **优化亮点**：将上述去噪的信号再次推进专门针对硬件的 **Cubic Lagrange Farrow 滤波器**，直接输出极其平滑没有混叠刺点毛刺的 SSS 基带子信号。

### Step 2.4: 动态偏移探针探测法
- **操作**：由于物理界面的采样跳跃，设计循环探针法从 $-20 \sim +20$ 采样点间执行滑窗取帧。每一层窗内取出整 `1024` 枚采样做正交频域变换（FFT）。

### Step 2.5: 频域四次方解卷绕斜率去偏 (Residual STO 消除)
**最为硬核的频域补偿设计！**
- 尽管我们有了不错的宏观基准点，但由于小数偏差 (STO) 会在 OFDM 的频域拉出一条破坏性的线性相移爬坡：
  1. 我们摘取两侧拥有真实能量的 **824个子频点**。
  2. 对其进行强行 **四次方乘方算子 ($x^4$)**，瞬间洗除承载的全部正反调制符号象限角。
  3. 通过强大的 **`unwrap`** 算法缝补缠绕翻转の角位跳崖。
  4. 利用一次线性回归 **`polyfit` 拟合** 得到这条误差斜线。
  5. 算出整网补数：$e^{-j \cdot \frac{P_{fit(f)}}{4}}$，全局倒灌补偿。
- 最终结果：屏幕上散乱得犹如繁星粉尘的星座轨迹点，立刻凝聚结成四个闪亮的、无残余频率漂移和相位偏置的 **完美 4QAM 星座原点**！

---

## 3. 项目可读核心源代码清单与使用向导

为了在您的计算机上百分百复现该令人震撼的链路运行效果，请保证完整签收并运行以下文件包：

### 📁 核心数据源文件 (必须具备)
- **`sigtest1.iq`** (或配套压缩版) —— 原始过采样带频偏基带信号录音包。无此文件如同巧妇难为无米之炊。

### 📁 解析与基建模块
- **`iq_read_int16_le.m`** —— 专门编写的适配底层小端序 `int16` 二进制流内存读装件，所有架构的基石源头。

### 📁 链路模块 A: 单载波主同步 PSS 探测与解调
- **`autocorr_test2_slide128.m` (或类似名称的探测主脚本)**
  *   **用途**：实现极早期的 PSS 自治粗定时盲搜，在极差信噪比下标定出 PSS 第一块 128 片段跳变沿并输出位置的探索器。
- **`gardner_farrow_timing_recovery.m`** 
  *   **用途**：接收前端定位后，它将自动捕获 M 序列，执行 Farrow+TED 定时校正、Costas相位锁频与差分解调，打印收敛状态与硬判决特征码。

### 📁 链路模块 B: 多载波辅同步 SSS (核心展示件)
- **`sss_demodulation.m`**
  *   **用途**：基于前部分测算参数的集成体。运行后可以在终端内**疯狂按回车 (Enter)** 检阅动态补偿对齐过程，亲眼看见原本散架的子载波云被彻底聚拢降服！
- **`estimate_sss_cp_60mhz.m` (选配测算件)** 
  *   **用途**：CP自干涉延迟共轭检测算法 (Delay and Multiply)。用来论证在解调 SSS 前确定的物理滑动偏移窗口最佳解算基点。
- **`plot_subcarrier_energy.m` (选配套件)**
  *   **用途**：时-频空间信号全景 X 光透视 CT 仪，利用 `1e-12` 抑制了数学极限，用来核查抽取的 824 点载波包络对齐度。
